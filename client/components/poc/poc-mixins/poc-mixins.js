( function( polymer ) {
  "use strict";

  polymer( "poc-mixins", {
    /* LIFECYCLE */
    ready: function() {},
    attached: function() {},
    detached: function() {},
    attributeChanged: function( attrName, oldVal, newVal ) {}
    /* PROPERTIES */
    /* METHODS */
  });
})( window.Polymer );
