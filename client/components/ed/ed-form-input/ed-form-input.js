( function( polymer ) {
  "use strict";

  polymer( "ed-form-input", {
    /* LIFECYCLE */
    publish: {
      wide: {
        reflect: true
      },
      high: {
        reflect: true
      }
    },
    ready: function() {},
    attached: function() {},
    detached: function() {},
    attributeChanged: function( attrName, oldValue, newValue ) {}
    /* PROPERTIES */
    /* METHODS */
  });
})( window.Polymer );
