( function( polymer ) {
  "use strict";

  polymer( "ed-star-rating", {
    /* LIFECYCLE */
    disable: false,
    ready: function() {},
    attached: function() {},
    detached: function() {},
    attributeChanged: function( attrName, oldValue, newValue ) {}
    /* PROPERTIES */
    /* METHODS */
  });

})( window.Polymer );
