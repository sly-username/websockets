language: node_js
node_js:
  - "0.12"
#  - "iojs"
env:
  - IS_TRAVIS=TRUE
before_install:
  - "rm -rf ./node_modules"
  - "mkdir ./logs"
  - "mkdir ./logs/gulp"
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
install:
  - "npm install --force"
