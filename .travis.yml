language: node_js
node_js:
  - "0.12"
#  - "iojs"
env:
  - GULP_ENVIRONMENT=DEVELOPMENT
  - IS_TRAVIS=TRUE
before_install:
  - "rm -rf ./node_modules"
  - "mkdir ./logs"
  - "mkdir ./logs/gulp"
install:
  - "npm install --force"
#before_script:
#  - "npm run gulp build"
